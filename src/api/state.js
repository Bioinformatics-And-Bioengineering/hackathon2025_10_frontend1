// import { api } from "./client.js";
// export async function getState({ month, userId }) {
//   const { data } = await api.get("/api/state", { params: { month, user_id: userId } });
//   return data; // { character, goals, calendar, entries }
// }
